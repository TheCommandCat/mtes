# Frontend Production Environment Variables
# ========================================
# This file contains environment variables for Docker deployment

# API Configuration
# Client-side requests (browser) - use localhost
NEXT_PUBLIC_API_URL=http://localhost:3333

# Server-side requests (SSR) - use Docker service name
NEXT_INTERNAL_API_URL=http://backend:3333

# WebSocket Configuration
# For Docker deployment, use the backend service name or external URL
# Examples:
# - Docker internal network: ws://backend:3333
# - External production URL: wss://api.yourdomain.com
NEXT_PUBLIC_WS_URL=ws://backend:3333